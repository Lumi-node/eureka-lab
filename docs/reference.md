# API Reference

`EurekaLab` is imported as `sandbox_science`. The public API:

```python
import sandbox_science
```

### `AuditFlag`

Data model.

### `AuditReport`

Data model.

### `Auditor`

Key methods: `cross_validate()`, `verify()`

### `CommitInfo`

Data model.

### `CostActual`

Data model.

### `CostEstimate`

Data model.

### `CostEstimator`

Key methods: `estimate()`, `update()`

### `CrossValReport`

Data model.

### `ExecutionSpec`

Data model.

### `Executor`

Key methods: `run()`

### `ExperimentRequest`

Data model.

### `ExperimentResult`

Data model.

### `GitRepo`

Key methods: `commit()`, `init()`, `log()`, `merge()`

### `MergeResult`

Data model.

### `PolicyDecision`

Data model.

### `PolicyEngine`

Key methods: `is_allowed()`, `record_spend()`

### `RunLog`

Data model.

### `Sandbox`

Key methods: `cancel()`, `submit()`

